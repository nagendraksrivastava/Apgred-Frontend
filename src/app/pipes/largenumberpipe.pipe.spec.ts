import { LargenumberpipePipe } from './largenumberpipe.pipe';

describe('LargenumberpipePipe', () => {
  it('create an instance', () => {
    const pipe = new LargenumberpipePipe();
    expect(pipe).toBeTruthy();
  });
});
